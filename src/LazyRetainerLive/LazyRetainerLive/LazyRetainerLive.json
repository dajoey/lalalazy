{
  "Author": "dajoey",
  "Name": "LazyRetainerLive",
  "InternalName": "LazyRetainerLive",
  "Description": "Serves the logged-in character's live retainer and venture state on loopback HTTP (127.0.0.1:10504) so the ffxiv dashboard relay can show real-time venture countdowns instead of AutoRetainer's occasionally-stale file cache.",
  "Punchline": "Real-time retainer venture countdowns for your dashboard.",
  "ApplicableVersion": "any",
  "DalamudApiLevel": 15,
  "Tags": ["retainers", "ventures", "dashboard", "http", "lazy"],
  "CategoryTags": ["utility"],
  "AcceptsFeedback": false,
  "LoadRequiredState": 0,
  "LoadSync": false,
  "CanUnloadAsync": false,
  "LoadPriority": 0,
  "IsTestingExclusive": false,
  "IconUrl": "https://raw.githubusercontent.com/dajoey/lalalazy/main/LalaImages/lazyretainerlive-icon.png",
  "Changelog": "v0.1.0.1 (2026-09-05)\n\n\n- Corrected a wrong claim in the v0.1.0.0 notes below (and on the mod's web page): **the endpoint does not answer 503 on logout.** Once logged in, it keeps serving that character's last known retainer state - including at the title screen - and only ever answers 503 if it has never read the retainer table at all since the plugin loaded (i.e. it started at the title screen and no login has happened yet). That freeze is deliberate and is the better behaviour: measured on 2026-09-05 the frozen live snapshot was still *newer* than AutoRetainer's file, so falling back to the file on logout would have shown the dashboard **older** data, not fresher. No behaviour was changed here - only the description was wrong (file: `HttpServer.cs`, `RetainerLiveService.cs`, `RetainerState.cs`).\n- The settings window no longer reports \"serving live snapshot\" while logged out. It now distinguishes three states: no snapshot yet, serving a live snapshot, or serving the last-known snapshot while the game is in transition (file: `ConfigWindow.cs`, new `RetainerLiveService.LastTickOk`).\n\n\n- For anything scripting against this endpoint: `readAt` is the time the request was **served**, not the time the data was read from the game, so it does not reveal how stale a frozen snapshot is. Use the settings window (or `/lazyretainerlive debug`) to tell live from frozen.\n\nv0.1.0.0 (2026-09-05)\n\n\n- First release. LazyRetainerLive reads the logged-in character's LIVE retainer table (the same in-memory data AutoRetainer uses to drive its own countdowns) and serves it as JSON on `http://127.0.0.1:10504/retainers`, so the ffxiv dashboard's retainer panel can show venture countdowns that update the moment a venture is collected instead of whenever AutoRetainer next saves its config file.\n- When not logged in (or the live table has no data yet) the endpoint answers HTTP 503, which tells the dashboard relay to fall back to AutoRetainer's file. Loopback only - the port is never reachable from other machines.\n\n\n- Companion of AutoRetainer - it reads, it never writes, assigns, or collects anything. Settings: Enabled (default on) and loopback port (default 10504). Commands: `/lazyretainerlive` opens settings, `/lazyretainerlive debug` dumps listener + snapshot state, `/lazyretainerlive changelog` reopens this popup.\n- Why this exists: AutoRetainer's `DefaultConfig.json` (which feeds the dashboard today) is saved only at AutoRetainer's own save points, so its venture rows can be tens of minutes stale while ventures are actually completing. The countdown for the logged-in character lives in the game's memory - this plugin is a small read-only window onto it."
}
